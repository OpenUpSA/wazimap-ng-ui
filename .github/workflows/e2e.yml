name: E2E
on:
  push:
    branches:
      - staging
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - name: Get yarn cache
        id: yarn-cache-dir-path
        run: echo "::set-output name=dir::$(yarn cache dir)"
      - name: Cypress run
        uses: cypress-io/github-action@v2.6.1
        with:
          browser: chrome
          headless: true
        env:
          CYPRESS_BASE_URL: https://wazimap-staging.netlify.app